# Workspace Compute

The workspace's server provides:

| Resource | Spec |
|---|---|
| CPU cores | `$AGENT_CPUS` |
| Memory per CPU | `$LEANMARATHON_SLURM_MEM_PER_CPU` MB |
| Total Memory | `$LEANMARATHON_TOTAL_MEM_MB` MB |
